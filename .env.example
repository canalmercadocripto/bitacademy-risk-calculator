# Configurações do Servidor
NODE_ENV=production
PORT=3001

# Configurações de Segurança
JWT_SECRET=SEU_JWT_SECRET_AQUI_MUITO_SEGURO_E_LONGO
ADMIN_EMAIL=admin@bitacademy.vip
ADMIN_PASSWORD=SUA_SENHA_ADMIN_SUPER_SEGURA

# Configurações do Banco de Dados
# Para PostgreSQL (produção)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=bitacademy
DB_USER=postgres
DB_PASSWORD=sua_senha_postgres

# Para desenvolvimento local (SQLite)
# Não precisa configurar - usa SQLite automaticamente
EOF < /dev/null
